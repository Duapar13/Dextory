services:
  dextory:
    build: .
    container_name: dextory
    restart: unless-stopped
    ports:
      - "8020:8020"
    volumes:
      - dextory-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=8020
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8020/auth/me"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  dextory-data:
